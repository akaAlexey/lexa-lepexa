# Статус выкладки «Тропы памяти»

Журнал ops-сессии (Cowork, 25.09.2026). Каждый шаг: что сделано, адреса, как откатить.

## Доступы в этой сессии
- Работа шла из облака: git (clone/push) — есть; браузер, Deploy-F, github.io, Actions UI, API — недоступны.
- Всё, что требует Deploy-F или настроек GitHub, вынесено в раздел «Осталось людям».

## Шаг 1 — сайт жюри (P0)
- `git revert 4b3f16a` в main: сборка Pages снова в режиме `vars.API_URL ? live : mock`.
  Причина: жёстко прописан `https://a49782-3772.q-d-f.pw/api/v1` — домена нет (опечатка q-d-f),
  по правильному q.d-f.pw `/health` и `/api/v1/graves` → 404.
- Сайт: https://team-shpilit.github.io/lexa-lepexa/ (демо-данные, если переменная API_URL не задана).
- **Не проверено из сессии**: результат Actions и сам сайт (облаку закрыт github.io). Проверить руками:
  роли «Семья» и «Командир» → «Тропа», «Поисковикам», «Последний бой» без ошибок загрузки.
  Если ошибки остались — проверить, не задана ли переменная репозитория API_URL (Settings → Variables).
- Откат: `git revert <sha этого коммита>` (вернёт жёсткий живой режим — не нужно).

## Шаг 2 — бэкенд из main (P0): код готов, выкладка — за людьми
- Из веток @moonwalkered перенесено только CORS: боевые origin (Pages, marshrutypobedy.ru, www) — в умолчаниях
  `backend/app/config.py`. Сид при старте в main уже идемпотентный (по id) — брать из веток нечего;
  «hide test sites» заменён очисткой базы (ниже), а не фильтром в API.
- `npm run contract:check -- <url> --read-only` — только GET + открытие SSE (20 из 20 на локальной базе).
- `docs/ops/smoke.sh <root>` — /health, счётчики всех GET, SSE 5 с, заголовок CORS.
- Тестовые записи: `backend/app/test_records.sql` (ПРОСМОТР / УДАЛЕНИЕ) и `CLEANUP_TEST_RECORDS=1` → `app.cleanup`.
- Проверено локально (Postgres 16): база в состоянии веток @moonwalkered (alembic 0002 + их сид + записи сверки) →
  старт из main: миграции 0003–0004 прошли, сид +75, read-only сверка 20/20; очистка нашла 18 тестовых записей,
  удалила, вернула сбор F01 и выезд W01 к демо-значениям. pytest 16/16, ruff, typecheck/lint/format фронта — ок.
- Архив для загрузки: `backend-deploy-f.zip` (передан в чат Cowork).
- Откат: в Deploy-F выложить предыдущую версию; код — `git revert` коммита шага 2.

## Шаг 3 — база: процедура в `backend/DEPLOY-F.md`
- Повторный старт на той же базе: `seed: добавлено записей — 0`, счётчики до/после совпали (без дублей).
- Счётчики демо: маршрут 1 (4 точки), отрядов 5, мест 3, сборов 5 (не 3 — так в фикстурах фронта), выездов 2,
  историй 3, памятников 84, «живых фото» 2.

## Шаги 4–5 — не начаты (условия не выполнены)
- Домен в Pages и `--base=/` — только после зелёных шагов 1–2; из этой сессии нет доступа к настройкам Pages,
  поэтому сборку на `--base=/` не переводили (без домена в Pages сайт бы пропал, как в 88f31f2).
- `API_URL` не задана — сайт на демо-данных. Задавать только после зелёного шага 2, HTTPS у api и решения «связывай».

## Итог на 25.09
| | Адрес | Состояние |
|---|---|---|
| Сайт | https://team-shpilit.github.io/lexa-lepexa/ | демо-режим (revert 4b3f16a), нужно подтвердить глазами |
| API (Deploy-F) | https://a49782-3772.q.d-f.pw | старый код веток @moonwalkered, 404 на /health |
| API (домен) | https://api.marshrutypobedy.ru | DNS → 62.109.7.48, к приложению не привязан |
| Домен сайта | https://marshrutypobedy.ru | DNS → GitHub Pages, в Pages не указан |

Откат: снять переменную `API_URL` (Settings → Variables) → одна сборка → демо; снять домен в Pages и вернуть
`--base=/lexa-lepexa/` (как в 17aaf4c); бэкенд — предыдущая версия в Deploy-F.

### Осталось людям
1. Открыть сайт: «Семья» и «Командир» → «Тропа», «Поисковикам», «Последний бой» — без ошибок. Actions — зелёные.
2. Deploy-F (по `backend/DEPLOY-F.md`): бэкап → загрузить `backend-deploy-f.zip` или ветку main/`backend/` →
   переменные → перезапуск → `bash docs/ops/smoke.sh https://a49782-3772.q.d-f.pw`.
3. Тестовые записи: блок ПРОСМОТР из `backend/app/test_records.sql` → согласовать → УДАЛЕНИЕ
   (или `CLEANUP_TEST_RECORDS=1` на один перезапуск).
4. Привязать `api.marshrutypobedy.ru` к приложению, выпустить сертификат.
5. Шаг 4 (домен сайта) и решение «связывать ли на защите» (шаг 5).
6. Отозвать GitHub-токен и сменить пароль Deploy-F — они были переданы в чат открытым текстом.
