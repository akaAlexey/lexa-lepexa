# Статус выкладки «Тропы памяти»

Журнал ops-сессии (Cowork, 25.09.2026). Каждый шаг: что сделано, адреса, как откатить.

## Доступы в этой сессии
- Работа шла из облака: git (clone/push) — есть; браузер, Deploy-F, github.io, Actions UI, API — недоступны.
- Всё, что требует Deploy-F или настроек GitHub, вынесено в раздел «Осталось людям».

## Шаг 1 — сайт жюри (P0)
- `git revert 4b3f16a` в main: сборка Pages снова в режиме `vars.API_URL ? live : mock`.
  Причина: жёстко прописан `https://a49782-3772.q-d-f.pw/api/v1` — домена нет (опечатка q-d-f),
  по правильному q.d-f.pw `/health` и `/api/v1/graves` → 404.
- Сайт: https://team-shpilit.github.io/lexa-lepexa/ (демо-данные, если переменная API_URL не задана).
- **Не проверено из сессии**: результат Actions и сам сайт (облаку закрыт github.io). Проверить руками:
  роли «Семья» и «Командир» → «Тропа», «Поисковикам», «Последний бой» без ошибок загрузки.
  Если ошибки остались — проверить, не задана ли переменная репозитория API_URL (Settings → Variables).
- Откат: `git revert <sha этого коммита>` (вернёт жёсткий живой режим — не нужно).
