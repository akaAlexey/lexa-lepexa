# Сборка для APK (npm run build:android): демо-данные в приложении, без service worker —
# в WebView Capacitor файлы и так лежат локально, а воркер мешал бы локальному серверу.
VITE_API_MODE=mock
VITE_OFFLINE=off
VITE_TILES=openfreemap
